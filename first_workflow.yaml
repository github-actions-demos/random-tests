name: First Workflow
on: [ push ]
jobs:
  run-shell-commands: #job-id
    run-ons: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: Multiline command
        run: |
          node -v
          npm -v
